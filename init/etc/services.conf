# ============================================================
# BlockOS Service Manager
# ============================================================

[service:logd]
path=/sbin/blockos-logd
restart=always
restart-delay=250
max-restarts=10
required=true

[service:shell]
path=/bin/blockos-shell
restart=on-failure
restart-delay=500
max-restarts=5
required=false