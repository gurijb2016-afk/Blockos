# Common options shared by the three builds.
export MESON_CROSS_FILE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)/blockos-x86_64.ini"
export MESON_NATIVE_FILE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)/blockos.ini"
export MESON_COMMON="--cross-file $MESON_CROSS_FILE"
