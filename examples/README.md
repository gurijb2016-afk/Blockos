# BlockOS examples

These examples are deliberately small and educational. They show how userspace programs consume the BlockOS POSIX layer.

Drivers are different: a real kernel driver must use BlockOS's driver/device framework. The `driver/` example therefore documents the integration path instead of inventing a fake driver ABI.
