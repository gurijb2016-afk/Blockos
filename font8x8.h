#pragma once
#include <stdint.h>

/*
 * Minimal placeholder 8x8 font table.
 * Currently all glyphs are empty (zeros). This prevents build errors
 * and can be replaced later with a full font (e.g. the standard font8x8_basic table).
 */
static const unsigned char font8x8_basic[128][8] = {{0}};
